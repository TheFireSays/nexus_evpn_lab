---
- name: Create VLANs
  hosts: leafs
  gather_facts: false

  tasks:
    - name: Create host VLANs
      cisco.nxos.nxos_vlans:
        config: "{{ vlans }}"
        state: merged

    - name: Create transit VLANs
      cisco.nxos.nxos_vlans:
        config: "{{ transit_vlans }}"
        state: merged
